{
  "tasks": {
    "start": "deno run -A --watch=static/,routes/ dev.ts",
    "compile-map": "deno run -A scripts/compileMap.ts",
    "create-radio-db": "deno run -A --unstable scripts/createRadioDb.ts",
    "check": "deno fmt && deno lint && fd -e ts -x deno check --unstable"
  },
  "importMap": "./import_map.json"
}
